; - Joypad Input -
JOY_RIGHT EQU 0
JOY_LEFT EQU 1
JOY_UP EQU 2
JOY_DOWN EQU 3
JOY_A EQU 4
JOY_B EQU 5
JOY_SELECT EQU 6
JOY_START EQU 7

;JOY_RIGHT EQU $1
;JOY_LEFT EQU $2
;JOY_UP EQU $4
;JOY_DOWN EQU $8
;JOY_A EQU $10
;JOY_B EQU $20
;JOY_SELECT EQU $40
;JOY_START EQU $80

; - Colors -
CLR_WHITE EQU 0
CLR_BLACK EQU $FF

; - Canvas Dimension Data -
CANVAS_WIDTH EQU 16
CANVAS_HEIGHT EQU 15

; - Screen Dimension Data -
SCREEN_TILES_WIDTH EQU 32
SCREEN_TILES_HEIGHT EQU 32

; - Memory Map -
MM_BG_CHAR_START EQU $8800
MM_BG_CHAR_LENGTH EQU $0FFF
MM_BG_CODES_START EQU $9800
MM_BG_CODES_LENGTH EQU $00FF

MM_SPRITE_CHAR_START EQU $8000

MM_SHADOW_OAM_START EQU $C000
MM_SHADOW_OAM_LENGTH EQU 40*4

; - Reticle OAM location -
RETICLE_OAM_ATTRIBUTES EQU MM_SHADOW_OAM_START + 0
RETICLE_OAM_Y EQU RETICLE_OAM_ATTRIBUTES + 0
RETICLE_OAM_X EQU RETICLE_OAM_ATTRIBUTES + 1

; - Function Prototype -
DMARoutine EQU $FF80

;- Shift Macros -
; register, value
rightShift: MACRO
 REPT \2
  srl \1
 ENDR
ENDM

leftShift: MACRO
 REPT \2
  rl \1
 ENDR
ENDM

rightShift16: MACRO
ENDM

leftShift16_4bm: MACRO
 push hl
 ld h, \1
 ld l, \2
 REPT \3
  add hl, hl
 ENDR
 ld \1, h
 ld \2, l
 pop hl
ENDM